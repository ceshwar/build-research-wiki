## Summary
Expose completion stats inside Obsidian (Dataview queries on frontmatter) so the Dive Computer dashboard is visible without opening the web UI.

## Acceptance
- [ ] Frontmatter fields on `wiki/papers/` and `wiki/sources/` for chart tier / status
- [ ] Example Dataview dashboard in Shallow reef demo
