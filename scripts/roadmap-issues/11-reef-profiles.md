## Summary
Per-lab **reef profiles**: which docks appear by default, which wiki sections are generated, custom completion rules.

## Acceptance
- [ ] Profile config (YAML or `builder/data.py` extension)
- [ ] `new_vault.py` scaffolds from profile template
